import React from 'react'
import {cpus} from '../assets/database/cpu.js'


const Main = () => {
  return (
    <div className='main'>
        {
          cpus.map((currentValue, index)=>{return `} 
        }
    </div>
  )
}

export default Main
